webdriver {
  protected driver = chrome
}

headless.mode = false

serenity {
  project.name = "Growth Automation Report"
  take.screenshots = AFTER_EACH_STEP
  test.root = "com.growth.automation"
  tag.failures = "true"
  linked.tags = "issue"
  logging = "NORMAL"
}